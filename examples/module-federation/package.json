{
	"name": "@example/module-federation",
	"scripts": {
		"dev": "pnpm run --stream --color \"/^dev:(remote|host)/\"",
		"dev:host": "pnpm --filter @example/module-federation-host run dev",
		"dev:remote": "pnpm --filter @example/module-federation-remote run dev"
	}
}
